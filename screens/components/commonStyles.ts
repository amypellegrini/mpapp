import {StyleSheet} from 'react-native';

const commonStyles = StyleSheet.create({
  h4: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#EFEFEF',
    marginBottom: 5,
  },
  h6: {
    color: '#EFEFEF',
    fontWeight: 'bold',
  },
  mb10: {
    marginBottom: 10,
  },
});

export default commonStyles;
